# sample hamburger menu with transitions 

jquery
sass

see js how to toggling

sample with jquery
	
	$('.menu').on('click', function () {
 		$('.menu').toggleClass('is-active');
  		$('#navigation').toggleClass('hide');
	});